on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          ls
      - uses: ./action.yml
        with:
          cmd: |
            uname
